# Assets

Esta carpeta guarda los archivos visuales y estáticos usados por la web.

- `images/`: fotos e imágenes de página.

Si cambias una imagen por otra con el mismo nombre, normalmente no hace falta tocar código.
